// ffmpeg.av — the whole FFmpeg surface in one import (Python `import av` analog).
export module ffmpeg.av;

export import ffmpeg.avutil;
export import ffmpeg.avcodec;
export import ffmpeg.avformat;
export import ffmpeg.avfilter;
export import ffmpeg.avdevice;
export import ffmpeg.swscale;
export import ffmpeg.swresample;
